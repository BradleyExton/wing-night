export const container =
  "relative grid h-full grid-rows-[auto_1fr_auto] gap-[clamp(1.5rem,3vw,3rem)] px-[clamp(2rem,4vw,4rem)] py-[clamp(2rem,4vw,4rem)]";

export const metaRow =
  "flex items-start justify-between text-[clamp(0.85rem,1.1vw,1.15rem)] font-semibold uppercase tracking-[0.28em] text-muted";

export const metaCol = "flex flex-col gap-[0.4rem]";

export const metaColRight = "items-end text-right";

export const metaAccent =
  "text-[clamp(1rem,1.4vw,1.5rem)] tracking-[0.2em] text-primary";

export const metaAccentTeam =
  "text-[clamp(1rem,1.4vw,1.5rem)] tracking-[0.2em] text-text";

export const timerArea =
  "flex flex-col items-center justify-center gap-[clamp(1rem,2vw,2rem)]";

export const timer =
  "m-0 font-mono text-[clamp(7rem,22vw,28rem)] font-black leading-[0.85] tabular-nums tracking-[-0.06em] text-primary";

export const timerUrgent = "text-heat animate-[heatpulse_0.65s_ease-in-out_infinite]";

export const timerLabel =
  "text-[clamp(1rem,1.4vw,1.6rem)] font-bold uppercase tracking-[0.32em] text-muted";

export const timerLabelTeam = "text-text";

export const heatTrack =
  "relative h-[clamp(8px,1vh,14px)] overflow-hidden rounded-full bg-text/[0.06]";

export const heatFill =
  "absolute inset-y-0 left-0 rounded-full bg-gradient-to-r from-gold via-primary to-heat shadow-[0_0_16px_rgba(249,115,22,0.4)] transition-[width] duration-1000 ease-linear";
