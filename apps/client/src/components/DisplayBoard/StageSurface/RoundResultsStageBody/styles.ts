export const container =
  "relative flex h-full flex-col items-center justify-center gap-[clamp(1.5rem,3vw,2.75rem)] overflow-hidden px-[clamp(1.5rem,3vw,3rem)] py-[clamp(1.5rem,3vw,3rem)] text-center";

export const ambient =
  "pointer-events-none absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(249,115,22,0.08)_0%,transparent_55%)]";

export const header =
  "relative flex flex-col items-center gap-[0.6rem]";

export const eyebrow =
  "text-[clamp(0.85rem,1.1vw,1.2rem)] font-bold uppercase tracking-[0.32em] text-muted";

export const heading =
  "m-0 text-[clamp(2rem,5.5vw,6.5rem)] font-black uppercase leading-[0.9] tracking-[-0.01em] text-text";

export const headingAccent = "text-primary";

export const table =
  "relative grid w-full max-w-[1100px] gap-0 border-t border-text/[0.08] bg-text/[0.04]";

export const tableHeader =
  "grid grid-cols-[2fr_1fr_1fr_1.2fr] items-center gap-[clamp(0.8rem,1.5vw,1.5rem)] bg-text/[0.025] px-[clamp(1rem,1.6vw,2rem)] py-[clamp(0.7rem,1.1vh,1.2rem)] text-left text-[clamp(0.65rem,0.8vw,0.95rem)] font-bold uppercase tracking-[0.18em] text-muted";

export const tableHeaderNum = "text-right";

export const tableRow =
  "relative grid grid-cols-[2fr_1fr_1fr_1.2fr] items-center gap-[clamp(0.8rem,1.5vw,1.5rem)] border-t border-text/[0.04] bg-bg px-[clamp(1rem,1.6vw,2rem)] py-[clamp(0.7rem,1.1vh,1.2rem)] text-left";

export const tableRowEdge =
  "pointer-events-none absolute inset-y-0 left-0 w-[3px]";

export const teamCell =
  "text-[clamp(1.1rem,1.55vw,2.1rem)] font-extrabold uppercase tracking-[0.04em] text-text";

export const num =
  "text-right font-mono text-[clamp(1.4rem,2vw,2.5rem)] font-black tabular-nums tracking-[-0.03em] text-text";

export const numMuted = "text-muted";

export const numTotal = "text-primary";

export const numTotalTop =
  "text-gold [text-shadow:0_0_18px_rgba(251,191,36,0.45)]";
