export const overlay =
  "pointer-events-none fixed inset-0 z-30 flex items-center justify-center bg-bg/88 px-6 py-8 backdrop-blur-sm";

export const panel =
  "flex w-full max-w-[min(70rem,92vw)] flex-col items-center justify-center rounded-[2rem] border border-primary/30 bg-surface/82 px-8 py-12 text-center shadow-[0_32px_120px_rgba(0,0,0,0.55)] md:px-14 md:py-16";

export const brand =
  "m-0 text-[clamp(0.95rem,1vw,1.25rem)] font-black uppercase tracking-[0.42em] text-primary/85";

export const title =
  "mt-5 text-[clamp(2.6rem,6vw,6.8rem)] font-black uppercase tracking-[0.08em] text-text";

export const description =
  "mt-4 max-w-[32rem] text-[clamp(1rem,1.55vw,1.75rem)] font-medium leading-tight text-text/80";

export const countdownStack = "mt-10 flex flex-col items-center";

export const countdownLabel =
  "m-0 text-[clamp(0.95rem,1.1vw,1.35rem)] font-black uppercase tracking-[0.28em] text-primary";

export const countValue =
  "mt-4 font-mono text-[clamp(6rem,18vw,15rem)] font-black leading-none tabular-nums text-primary";
