export const footer =
  "shrink-0 border-t border-text/10 bg-surface px-[clamp(1rem,2.2vw,3rem)] py-[clamp(0.75rem,1.2vh,1.5rem)]";

export const heading =
  "m-0 text-[clamp(0.85rem,0.9vw,1.45rem)] font-semibold uppercase tracking-[0.16em] text-muted";

export const emptyLabel = "mt-3 text-[clamp(0.95rem,1vw,1.55rem)] text-muted";

export const standingsList = "mt-3 grid gap-[clamp(0.5rem,0.8vw,1rem)] md:grid-cols-3";

export const standingCardBase =
  "rounded-[clamp(0.4rem,0.8vw,0.85rem)] border bg-surfaceAlt px-[clamp(0.6rem,1vw,1.15rem)] py-[clamp(0.5rem,0.9vh,1rem)]";

export const standingCard = `${standingCardBase} border-text/10`;

export const leadingStandingCard = `${standingCardBase} border-primary/60 bg-primary/10`;

export const winnerStandingCard = `${standingCardBase} border-gold/70 bg-gold/10`;

export const teamColorEdge = "border-l-[0.35rem]";

export const teamRow = "flex items-start justify-between gap-3";

export const teamIdentity = "flex min-w-0 items-center gap-2.5";

export const teamAccentDot = "mt-1 h-2.5 w-2.5 shrink-0 rounded-full";

export const teamName =
  "m-0 min-w-0 truncate text-[clamp(1rem,1.05vw,1.7rem)] font-semibold text-text";

export const score =
  "m-0 whitespace-nowrap text-[clamp(0.82rem,0.85vw,1.3rem)] text-text/80";

export const teamRoster =
  "mt-1.5 text-[clamp(0.8rem,0.8vw,1.22rem)] leading-tight text-text/78";
