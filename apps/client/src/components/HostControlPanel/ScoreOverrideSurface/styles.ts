import {
  actionButtonPrimary,
  cardBase,
  fieldLabel as fieldLabelToken,
  inputBase,
  sectionDescriptionDefault as sectionDescriptionDefaultToken,
  sectionHeading as sectionHeadingToken,
  selectBase
} from "../styleTokens";

export const card = cardBase;

export const sectionHeading = sectionHeadingToken;

export const sectionDescription = sectionDescriptionDefaultToken;

export const form = "mt-4 flex flex-wrap items-end gap-3 px-1.5";

export const field = "flex min-w-[160px] flex-1 flex-col gap-1.5";

export const fieldLabel = fieldLabelToken;

export const select = selectBase;

export const input = inputBase;

export const actionButton = actionButtonPrimary;

export const emptyLabel = "mt-3 px-1.5 text-sm text-muted";
