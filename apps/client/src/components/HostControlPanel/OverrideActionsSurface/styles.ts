import {
  cardBase,
  sectionDescriptionDefault as sectionDescriptionDefaultToken,
  sectionHeading as sectionHeadingToken
} from "../styleTokens";

export const card = cardBase;

export const sectionHeading = sectionHeadingToken;

export const sectionDescription = sectionDescriptionDefaultToken;

export const actionRow = "mt-4 flex flex-wrap items-center gap-2";

export const actionButton =
  "min-h-11 rounded-md border border-text/20 bg-surface px-4 text-sm font-semibold text-text transition hover:border-primary/70 hover:text-primary focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary disabled:cursor-not-allowed disabled:opacity-40";
