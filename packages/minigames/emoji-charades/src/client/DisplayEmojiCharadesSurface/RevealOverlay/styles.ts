// The 2s verdict window (DESIGN.md §2.6). It is the house `<ResultPlaque>`, not
// a full-bleed wash: the clue is held on the board behind it and only dimmed,
// so the room reads the emoji and the answer in the same beat. The card brings
// its own entrance, so this wrapper only places it.
export const overlay = "absolute inset-0 z-10 grid place-items-center px-[8%]";
