// The shudder a stressed leg runs while a shot is leaning on it. Same idiom as the flight
// effects: one helper class, so the strain reads as part of the same scene language.
export const strain = "motion-safe:animate-pulse";
