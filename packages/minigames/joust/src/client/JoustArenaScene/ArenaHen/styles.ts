// A bird felled on an earlier shot keeps its team's colour — the room still
// wants to know who that is — and just reads as spent.
export const down = "opacity-50";

// The wing on its own layer wears the cast's own ink — the bird's colour, the house `bg` outline
// at the figure's 2-unit weight — so it is the wing the figure would have drawn, just turned. The
// cast keeps its silhouette class to itself, which is why the four words are repeated here.
export const wingInk = "fill-current stroke-bg stroke-2 [stroke-linejoin:round]";
