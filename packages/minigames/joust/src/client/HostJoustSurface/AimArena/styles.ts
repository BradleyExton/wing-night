// touch-none stops the tablet from scrolling or zooming mid-pull; the pointer
// is captured so a finger that slides off the arena still releases the shot.
export const surface = "h-full w-full touch-none";

export const surfaceArmed = "cursor-grab active:cursor-grabbing";

export const surfaceLocked = "cursor-default";
