export const introContainer =
  "flex h-full flex-col items-center justify-center gap-[clamp(1rem,2vw,2rem)] px-[clamp(1rem,3vw,3rem)] py-[clamp(1rem,2vw,2rem)] text-center";

export const introText =
  "m-0 text-[clamp(1.4rem,2.4vw,2.6rem)] font-semibold uppercase tracking-[0.18em] text-muted";

export const container =
  "flex h-full min-h-0 flex-col items-center justify-center gap-[clamp(0.9rem,1.8vw,1.8rem)] px-[clamp(1rem,3vw,3rem)] py-[clamp(0.75rem,1.5vw,1.5rem)] text-center";

export const title =
  "m-0 max-w-[18ch] text-balance text-[clamp(2.2rem,4.5vw,5rem)] font-black leading-[1.05] tracking-[-0.02em] text-text";

export const photo =
  "min-h-0 w-[min(72vw,64rem)] flex-1 rounded-[2rem] border border-text/10 object-cover";

export const hint =
  "m-0 text-[clamp(1.1rem,1.8vw,1.8rem)] font-semibold text-muted";

export const statusRow =
  "m-0 text-[clamp(1rem,1.6vw,1.6rem)] font-bold uppercase tracking-[0.28em] text-text";

export const statusTeamName = "text-primary";

export const statusLabel = "ml-[0.6em] font-medium text-muted";

export const mapShell =
  "h-[48vh] w-[min(80vw,72rem)] overflow-hidden rounded-[2rem] border border-text/10";

export const statsRow = "flex flex-wrap items-center justify-center gap-[clamp(1rem,2.5vw,2.5rem)]";

export const statBlock =
  "flex min-w-[12rem] flex-col gap-1 rounded-[1.5rem] border border-text/10 bg-surface px-[clamp(1.25rem,2vw,2rem)] py-[clamp(0.75rem,1.2vw,1.2rem)]";

export const statLabel =
  "text-[clamp(0.8rem,1vw,1rem)] font-semibold uppercase tracking-[0.22em] text-muted";

export const statValue =
  "text-[clamp(1.8rem,3vw,3.2rem)] font-black leading-none text-text";

export const pinLegend = "flex items-center justify-center gap-[clamp(1rem,2vw,2rem)]";

export const pinLegendEntry =
  "flex items-center gap-2 text-[clamp(0.9rem,1.2vw,1.2rem)] font-semibold uppercase tracking-[0.18em] text-muted";

export const guessPinDot =
  "inline-block h-[0.85em] w-[0.85em] rounded-full bg-primary";

export const answerPinDot =
  "inline-block h-[0.85em] w-[0.85em] rounded-full bg-success";
