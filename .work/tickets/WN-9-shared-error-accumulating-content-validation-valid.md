---
# ─── Required ───────────────────────────────────────────────────────────────
id: WN-9
title: "Shared error-accumulating content validation (validateGameConfigFile + file validators)"
status: done
kind: feature
priority: medium
created: 2026-08-05

# ─── Optional (delete a line to take its default) ───────────────────────────
deps: []                 # list<id>; DAG edges; must all be `done` before the SELECTOR picks this; default []
blocked_by: []           # list<string>; external/manual waits (free text); non-empty => selector skips; default []
# model: sonnet          # opus | sonnet | haiku; unset => global default-by-kind policy (SCHEMA §5)
# thinking: medium       # low | medium | high; unset => policy
# trust: checkpointed    # checkpointed | heads-down; default checkpointed
# needs_prototype: false # true => prototype must complete before in-progress; default false
# landing: preview-pr    # preview-pr | direct-main | feature-flag; unset => manifest default (SCHEMA §7)
# worktree:              # set by work-on on claim (collision guard); default null
# parallel_safe:         # RESERVED for F-8 (post-MVP) — do not set
worktree: "/Users/bradleyexton/Projects/wing-night-WN-9"
landed_range: 4195870c8a65db9d0b45ee5f3e1550601d475b98..efd844d85acc009f76f95f68a6635bca0e32995f
review: pending
---

## Goal
Add an error-accumulating validation API for the **five** editable content surfaces in
packages/shared — gameConfig, players, teams, the trivia pack, the drawing pack — keeping the
existing boolean guards as thin delegates (single source of truth, no fork). Geo is explicitly NOT
one of them (authored by `import:geo`; outer-shape guard only, unchanged by this ticket).

## Acceptance Criteria
- [ ] `validateGameConfigFile(value, options?): ValidationIssue[]` exists in `packages/shared/src/content/gameConfig/` (the optional second parameter is the rules seam specified below) where `ValidationIssue = { path: string; message: string }` (e.g. `{ path: "rounds[1].sauce", message: "must be a non-empty string" }`), covering every rule the current guard enforces: name, non-empty contiguous rounds, per-round fields, minigame membership, scoring positives, all required timer keys, outer minigameRules shape, setupPreviewRoundSlots bounds.
- [ ] `isGameConfigFile` is rewritten as `validateGameConfigFile(value).length === 0` — the predicate and the validator can never disagree; no behavior change for existing callers (contentLoader keeps working).
- [ ] Equivalent issue-returning validators exist for `players.json` (`{ players: [{name, avatarSrc?}] }`), `teams.json` (`{ teams: [{name}] }`), and the trivia (`{id,question,answer}`) + drawing (`{id,prompt}`) prompt packs, with their boolean guards (where they exist) delegating the same way. **Parity is exact — every rule the current guard enforces, not just the item shape.** For both packs that includes a non-empty `prompts` array AND unique prompt ids (`trivia/index.ts` `hasUniquePromptIds`, `drawing/index.ts` equivalent). A delegating boolean that accepts input today's guard rejects is a defect, not a simplification.
- [ ] **The plugin-rules seam is explicit.** `validateGameConfigFile` takes an optional injected rules validator — `validateGameConfigFile(value, options?: { validateRules?: (rulesKey: string, rules: unknown) => boolean })` — and emits one generic issue at `minigameRules.<key>` when it returns false. It **must not** import the plugin registry: `packages/shared` has zero dependencies and `packages/minigames/core` depends on `@wingnight/shared`, so calling `resolveMinigameRuntimePlugin` from shared would invert the dependency direction. With the option omitted, no rules issues are emitted — which is what keeps AC2's "no behavior change" true, since today's `isGameConfigFile` does not check rules either.
- [ ] `apps/server/src/contentLoader/loadGameConfig/index.ts` **delegates** to that seam rather than keeping a second copy of the rule: `assertMinigameRulesAreValid` currently walks `MINIGAME_TYPES`, resolves each plugin and throws. Rewrite it to pass a plugin-backed `validateRules` into `validateGameConfigFile` and throw if any issue comes back, preserving the existing error-message shape (`Invalid game config content at "<path>": minigameRules.<key> failed <type> rules validation.`) so boot-time fail-fast behavior is unchanged. Extending the plugin contract itself (per-rules issue paths instead of a boolean) stays out of scope.
- [ ] Colocated unit tests cover: a fully-valid config returns `[]`; each rule violation produces an issue whose `path` points at the offending field; multiple violations accumulate (not first-failure-only); non-contiguous round numbers report the index; an injected `validateRules` returning false yields exactly one `minigameRules.<key>` issue and omitting the option yields none; **and a duplicate-prompt-id pack is rejected** (no test anywhere in the repo covers that today).
- [ ] **`packages/shared` gains a runtime test runner**, without losing its type-level tests. Its `test` script is currently `tsc --noEmit -p tsconfig.test.json`, which compiles only `src/**/*.test-d.ts` — so a new `index.test.ts` would neither run nor typecheck, and `pnpm test` would go green having executed zero of this ticket's tests. Wire it to run BOTH, following the `packages/minigames/core` precedent: `"test": "tsc --noEmit -p tsconfig.test.json && tsx --test src/**/*.test.ts"`, adding `tsx` and `@types/node` from the workspace catalog (`catalog:` — pnpm-workspace.yaml pins tsx ^4.19.2, @types/node ^24.0.0) plus the resulting lockfile change. The `.test-d.ts` type tripwires must keep running — WN-12 depends on one of them.
- [ ] The new validators and the `ValidationIssue` type are re-exported from the shared barrel (`packages/shared/src/index.ts`), where the existing guards are already surfaced — WN-10 and WN-11 consume them from there.
- [ ] `pnpm typecheck` and `pnpm test` pass — and the new `packages/shared` tests are **observably executed** (the run reports them, not just zero failures).

## Plan
Grilled 2026-08-05 (plan-work session; user at the table — decisions user-confirmed unless marked).

- Location: next to the existing guards in `packages/shared/src/content/` — they ARE the schema; the validator becomes the primary implementation and the boolean guard delegates. Never fork the rules.
- Issue shape `{ path, message }` chosen so the wizard (WN-11) can map issues to form fields by path string; no i18n/severity in v1.
- Follow house style: promote `gameConfig.ts`-style modules per code-design rules if a file grows past coherence; colocate tests.
- Out of scope: geo pack validation beyond outer shape (authored by `import:geo`), plugin-owned per-rules schemas.

Re-planned 2026-08-07 after the gate1 park (WN-9.gate1.json). Both majors were verified against the
code before answering, not taken on the critic's word:

- **Test runner (major 1).** Confirmed: `packages/shared/package.json` has `"test": "tsc --noEmit -p
  tsconfig.test.json"`, `tsconfig.test.json` includes only `src/**/*.test-d.ts`, devDeps are
  `{typescript}` alone, and there are zero `.test.ts` files in the package. AC5's tests would have
  been dead code under a green gate — the exact false-green this system exists to catch. Fix is the
  `&&`-chained script above, deliberately keeping the type tests rather than replacing them.
- **Plugin-rules seam (major 2).** Confirmed the dependency direction: `assertMinigameRulesAreValid`
  (loadGameConfig/index.ts) resolves plugins and throws, and shared cannot reach it. Chose an
  **injected option** over the alternatives — a shared→plugins dependency inverts the graph, and a
  second copy of the rule in the wizard path forks the schema the Goal forbids. Defaulting the option
  to absent is what preserves AC2; the server opting in is what keeps one implementation.
- **Pack parity (minor).** Confirmed `hasUniquePromptIds` + the non-empty check exist in the trivia
  guard and have no test coverage; AC3 now demands parity explicitly and AC5 adds the regression test.
- Downstream note for WN-10: the `validateRules` option is the same seam its `config:save`/`config:apply`
  path should use, so a wizard-saved config is validated exactly as a boot-loaded one is.

## Progress
<the executing agent appends here — the restart-safe log>
- 2026-08-07T02:35:09.587Z gate1 (product-owner critic): needs-changes — recorded at .work/verdicts/WN-9.gate1.json. Summary: "Sound, well-formed, worth-doing slice, but two planning gaps must be closed first: packages/shared has no runtime test runner (so the mandated colocated tests would never execute under `pnpm test`), and AC4's plugin-rules issue has no specified seam given shared cannot depend on the minigame plugins." MAJOR 1 (machine-checkable-finish, ok:false): packages/shared/package.json test script is `tsc --noEmit -p tsconfig.test.json` over `src/**/*.test-d.ts` only, and devDeps are {typescript} — so the AC5 colocated unit tests would never execute and `pnpm test` (AC6) would go green having run zero of them. Needs an explicit AC/plan step wiring shared's runtime test script + tsx/@types/node devDeps (precedent: packages/minigames/core `tsx --test src/**/*.test.ts`). MAJOR 2 (hidden-constraints, ok:false): AC4 is not implementable as written — packages/shared has no dependencies and packages/minigames/core depends on @wingnight/shared, so validateGameConfigFile cannot call resolveMinigameRuntimePlugin; the plugin-rules check today lives in apps/server/src/contentLoader/loadGameConfig/index.ts:23-48 and throws. AC4 also collides with AC2 ("no behavior change"): emitting rules issues by default makes the predicate strictly stricter than today. The ticket must name the seam (e.g. an injected validateRules/isRulesByKey option supplied by the server) and say whether loadGameConfig delegates or keeps its own throw. MINOR: AC3 describes trivia/drawing packs by item shape only, but the existing guards also enforce non-empty prompts + unique prompt ids (trivia/index.ts:34-37,52-58; drawing/index.ts:29-32,47-53) — write parity language + a duplicate-id regression test, which nothing in the repo covers today. INFO: re-export the new validators + ValidationIssue from the shared barrel (packages/shared/src/index.ts) for WN-10/WN-11; and the Goal says "four editable content files" while the ACs enumerate five validators — pin the count. Routing per ship-next gate1: explicit rejection ⇒ demote ready → needs-planning and re-plan via plan-work. Pipeline halted before implement; no code was written.
- 2026-08-07T04:46:31.570Z gate1 (product-owner critic, re-judgement after the 2026-08-07 re-plan): PASS — recorded at .work/verdicts/WN-9.gate1.json (attempt 2, superseding the needs-changes grade). All six rubric checks ok:true, confidence high. Both prior majors verified CLOSED against the code, not taken on the ticket's word: (1) runtime test runner — catalog pins confirmed exact (pnpm-workspace.yaml tsx ^4.19.2, @types/node ^24.0.0), the packages/minigames/core precedent confirmed working (pnpm test executes 6 tests green), and the &&-chain preserves the .test-d.ts tripwires; root 'pnpm test' is 'pnpm -r --if-present test' so shared's new script is picked up automatically. (2) plugin-rules seam — dependency direction re-verified independently (shared devDeps {typescript} only; minigames/core depends on @wingnight/shared), the injected validateRules option respects it, and defaulting it absent preserves AC2. Prior MINOR (pack parity) also closed. TWO MINORS carry into implementation as guidance: (a) AC7's UNQUOTED 'tsx --test src/**/*.test.ts' is fragile — /bin/sh collapses ** to *, so it works only because all five validators sit at src/content/<surface>/ (depth 3, no shell match, literal passes through to node). The moment any test file lands at src/<x>/index.test.ts (depth 1) sh expands to that ONE file and every src/content/** test is silently skipped while the run still reports passes. USE THE QUOTED FORM the repo already uses in apps/server/package.json: tsx --test "src/**/*.test.ts". (b) AC5's error-message parity is unpinned — the existing test at apps/server/src/contentLoader/loadGameConfig/index.test.ts:116-157 asserts only /Invalid game config content/, which also matches the generic shape-failure message, so dropping the 'minigameRules.<key> failed <type> rules validation.' tail would pass today's suite; assert the FULL message. Also pin the iteration domain: today assertMinigameRulesAreValid walks MINIGAME_TYPES, so unknown minigameRules keys are silently ignored — iterating the config's own keys instead would be an AC2-forbidden behavior change. INFO: consider pinning "types": ["node"] in packages/shared/tsconfig.json to match the minigames/core precedent. Pipeline advances to implement.
- 2026-08-07T04:46:35.060Z prototype: skipped (not in plan) — work ship-plan WN-9 --json emits no prototype phase (needsPrototype: false).
- 2026-08-07T04:49:35.600Z claimed → in-progress @ /Users/bradleyexton/Projects/wing-night-WN-9
- 2026-08-07T04:53:59.993Z Implementation pass 1 complete (tests next). Structure: split packages/shared/src/content/gameConfig/index.ts into a thin entry barrel over minigameDefinitions/ (registry + derived types + resolvers) and validateGameConfigFile/ (file schema types + validator + isGameConfigFile delegate). The split is FORCED, not cosmetic: the validator needs MINIGAME_TYPES at module-eval time (REQUIRED_TIMER_KEYS is a top-level computed const), so leaving the definitions in the barrel that re-exports the validator would be a TDZ crash on a real ESM import cycle, not just untidy. Entry path unchanged, so socketEvents/, roomState/ and the package barrel needed no edits. New: content/validationIssue/ (the ValidationIssue type + prefixIssuePaths helper that lifts entry-relative paths into file coordinates) and content/promptPack/ (validatePromptPackFile — trivia and drawing are the SAME file shape over different prompt bodies, so the injected per-prompt validator is a real two-caller seam, not a speculative one). Parity was mirrored deliberately, not re-derived: (a) isObjectLike stays array-permissive because the guards it replaces used bare typeof/null checks that accepted arrays, and tightening would reject content the loader takes today; (b) players' avatarSrc keeps the 'in' presence test rather than !== undefined, because { avatarSrc: undefined } has always been rejected; (c) the rules seam iterates the registry's OWN rulesKeys rather than the config's keys, preserving today's behavior where an unrecognised minigameRules key is silently ignored. Per-index round contiguity (expected index+1) replaces the all-or-nothing hasContiguousRoundNumbers, which is equivalent as a boolean and satisfies AC6's report-the-index requirement. Server: assertMinigameRulesAreValid now delegates to the seam via a plugin-backed isRulesValidForKey, with a local MINIGAME_TYPE_BY_RULES_KEY inverse map recovering <type> for the unchanged error message. Applied both gate1 minors: package.json uses the QUOTED glob (tsx --test "src/**/*.test.ts") per the apps/server precedent, not the unquoted minigames/core one; tsconfig.json pins types: ["node"]. Added tsx + @types/node from catalog.
- 2026-08-07T04:58:15.201Z Verify GREEN (3 steps: lint, typecheck, test — lint is now in the default gate, WN-3 having landed). AC8 evidence: root 'pnpm test' reports 'packages/shared test: tests 50 / pass 50 / fail 0' — the new tests are OBSERVABLY EXECUTED, not silently skipped, which was gate1's major 1. AC7 evidence: 'tsc -p tsconfig.test.json --listFiles | grep test-d' lists all 8 .test-d.ts tripwires still compiling (gameConfig, geo, players, teams, trivia, roomState, socketClientRole, socketEvents) — WN-12's dependency is intact. Full suite green across all 7 workspace projects (shared 50, minigames/core 6, trivia 12, geo 20, drawing 25, server 157, client 194). Anti-blind-spot sweep (work grep --since main): 93 touched symbols. The large call-site counts (GameConfigFile 32, GameConfigRound 19, MinigameType 121) are TYPE references only — those type definitions moved file but are byte-identical, and a green cross-workspace typecheck is the proof. The behaviorally-live surface is the nine runtime guards; I opened every call-site of them: apps/server/src/contentLoader/loadGameConfig (rewritten here), loadPlayers/index.ts:24 and loadTeams/index.ts:25 (both use the guard as a bare boolean + type predicate before reading .players/.teams), and packages/minigames/{trivia,drawing}/src/runtime/content/index.ts, which pass isContentFile/isPrompt into createPromptContentAdapter — again boolean-only consumption. No call-site reads anything beyond the predicate, so the delegate rewrite is invisible to all of them. Note: the sweep's 'isNonEmptyString — 15 call-sites' hit is a false positive; packages/minigames/drawing/src/runtime/guards/index.ts declares its own local copy of that name and does not import mine.
- 2026-08-07T05:07:43.754Z qa (qa-reviewer agent, fresh context, sonnet for model diversity vs the opus implementer): PASS — recorded at .work/verdicts/WN-9.qa.json (attempt 1), graded sha b00e29bb2f1e332c45feb92f58ddf8b81f8ce22c which matches worktree HEAD exactly. All 13 checks ok:true, confidence high. It independently re-ran work verify, the full suite, and work grep --since main rather than trusting the progress log, and hand-diffed all nine boolean delegates rule-by-rule against 'git show main:...' — the central false-accept/false-reject risk — finding parity in both directions, including the three documented-deliberate choices (array-permissive outer shape, avatarSrc in-presence, rules-key iteration domain). It also traced a case I had not explicitly reasoned about: with MULTIPLE rules keys failing at once, the new throw-loop reproduces the old code's 'first minigameType in registration order wins' ordering. Three advisory Rubric B findings ride into evidence rather than looping (per the work-on contract, minor/info quality findings do not block and are not loop fodder): MINOR — promptPack/ and validationIssue/ have no colocated index.test.ts, covered only indirectly through consumer tests (placement gap, not a correctness gap); MINOR — the one-line isObjectLike predicate is duplicated across six files when validationIssue/ already exists as a home for it; INFO — validateGameConfigFile/index.ts is 278 lines, past the ~150-line prompt, though defensible as one coherent schema module. All three are real and worth a follow-up; none changes behavior.
- 2026-08-07T05:07:49.113Z browser-verify: skipped (non-UI) — ticket kind is 'feature' and the change is entirely non-visual (packages/shared validation modules + a server content-loader seam + package test wiring); no client surface is touched, so readBrowserOutcome routes to skip. Recorded BEFORE the handoff so the attested tree and the landed tree stay identical.
- 2026-08-07T05:08:03.548Z handed off → in-review (verify green); awaiting land
- 2026-08-07T05:09:02.476Z auto-landed on green verdicts + attestation (in-review → done); review: pending

## Evidence
<test output + screenshot / preview URL, recorded before `done`>

<!-- captured-evidence:start -->
**Verify gate:** ✓ PASS (3 step(s))

```
✓ lint: pnpm lint
✓ typecheck: pnpm typecheck
✓ test: pnpm test
```

**Anti-blind-spot grep:** 33 symbol(s) with external call-sites reviewed:

- `GameConfigFile` → apps/client/src/components/HostControlPanel/ConfigSetupPrototype/sampleDraft.ts:4, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/sampleDraft.ts:9, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:4, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:15, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:20, apps/client/src/copy/minigameBriefings.ts:1, apps/client/src/copy/minigameBriefings.ts:14, apps/client/src/copy/minigameBriefings.ts:29, … 24 more (run `work grep`)
- `GameConfigRound` → apps/client/src/components/DisplayBoard/StageSurface/RoundIntroStageBody/index.test.tsx:4, apps/client/src/components/DisplayBoard/StageSurface/RoundIntroStageBody/index.test.tsx:8, apps/client/src/components/DisplayBoard/StageSurface/RoundIntroStageBody/index.tsx:1, apps/client/src/components/DisplayBoard/StageSurface/RoundIntroStageBody/index.tsx:7, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:3, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:43, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:47, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:4, … 11 more (run `work grep`)
- `GameConfigScoring` → packages/shared/src/content/gameConfig/index.test-d.ts:5, packages/shared/src/content/gameConfig/index.test-d.ts:31, packages/shared/src/content/gameConfig/index.test-d.ts:67
- `GameConfigTimers` → packages/shared/src/content/gameConfig/index.test-d.ts:6, packages/shared/src/content/gameConfig/index.test-d.ts:42, packages/shared/src/content/gameConfig/index.test-d.ts:68, packages/shared/src/content/gameConfig/index.test-d.ts:83
- `MINIGAME_API_VERSION` → apps/client/src/utils/hostRequests/index.test.ts:6, apps/client/src/utils/hostRequests/index.test.ts:110, apps/client/src/utils/hostRequests/index.ts:3, apps/client/src/utils/hostRequests/index.ts:158, apps/server/src/socketServer/registerRoomStateHandlers/index.ts:3, apps/server/src/socketServer/registerRoomStateHandlers/index.ts:167, apps/server/src/socketServer/registerRoomStateHandlers/minigameTimerAuthorization.test.ts:6, apps/server/src/socketServer/registerRoomStateHandlers/minigameTimerAuthorization.test.ts:33, … 10 more (run `work grep`)
- `MINIGAME_DEFINITIONS` → apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:6, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:82, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:197, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantB.tsx:5, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantB.tsx:220, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:6, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:147, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:382, … 4 more (run `work grep`)
- `MINIGAME_TYPES` → apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:6, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:185, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantB.tsx:5, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantB.tsx:208, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:6, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:135, apps/client/src/minigames/registry/index.test.ts:5, apps/client/src/minigames/registry/index.test.ts:16, … 8 more (run `work grep`)
- `MinigameApiVersion` → packages/shared/src/socketEvents/index.ts:1, packages/shared/src/socketEvents/index.ts:7, packages/shared/src/socketEvents/index.ts:25
- `MinigameRules` → packages/shared/src/content/gameConfig/index.test-d.ts:3, packages/shared/src/content/gameConfig/index.test-d.ts:47, packages/shared/src/content/gameConfig/index.test-d.ts:58, packages/shared/src/content/gameConfig/index.test-d.ts:69, packages/shared/src/content/gameConfig/index.test-d.ts:86
- `MinigameType` → apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:2, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:1, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:4, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:45, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:4, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:35, … 74 more (run `work grep`)
- `SETUP_PREVIEW_ROUND_SLOTS_MAX` → apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:2, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:27, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:5, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:113, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:117, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:118
- `also` → apps/client/src/components/HostControlPanel/index.tsx:30, tools/playwright-ports/index.mjs:14
- `config` → apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:123, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:248, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:297, apps/client/src/components/DisplayBoard/index.test.tsx:51, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:3, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantC.tsx:424, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/index.tsx:1, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/index.tsx:36, … 8 more (run `work grep`)
- `content` → apps/client/src/components/DisplayBoard/index.test.tsx:35, apps/client/src/components/DisplayBoard/index.test.tsx:40, apps/client/src/components/DisplayBoard/index.test.tsx:47, apps/client/src/components/DisplayBoard/index.tsx:41, apps/client/src/components/DisplayBoard/styles.ts:10, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/sampleDraft.ts:1, apps/client/src/components/HostControlPanel/OverrideDock/index.tsx:116, apps/client/src/components/HostControlPanel/OverrideDock/styles.ts:38, … 249 more (run `work grep`)
- `contentRoot` → apps/server/src/contentLoader/index.test.ts:48, apps/server/src/contentLoader/index.test.ts:51, apps/server/src/contentLoader/index.test.ts:58, apps/server/src/contentLoader/index.test.ts:64, apps/server/src/contentLoader/index.test.ts:66, apps/server/src/contentLoader/index.test.ts:70, apps/server/src/contentLoader/index.test.ts:72, apps/server/src/contentLoader/index.test.ts:78, … 58 more (run `work grep`)
- `gameConfig` → apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:13, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:19, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:21, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:33, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:36, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:37, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:38, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:64, … 157 more (run `work grep`)
- `hasUniquePromptIds` → packages/shared/src/content/geo/index.ts:74, packages/shared/src/content/geo/index.ts:96
- `ids` → apps/client/src/utils/hostRequests/index.test.ts:230, apps/client/src/utils/hostRequests/index.test.ts:234, apps/client/src/utils/hostRequests/index.test.ts:246, apps/client/src/utils/resolveTeamColorVariant/index.test.ts:13, apps/client/src/utils/resolveTeamColorVariant/index.test.ts:23, apps/server/src/contentLoader/loadTeams/index.test.ts:7, packages/minigames/drawing/src/runtime/content/index.ts:12, packages/minigames/drawing/src/runtime/index.test.ts:126, … 4 more (run `work grep`)
- `isDrawingPrompt` → packages/minigames/drawing/src/runtime/content/index.ts:3, packages/minigames/drawing/src/runtime/content/index.ts:14
- `isNonEmptyString` → packages/minigames/drawing/src/runtime/guards/index.ts:34, packages/minigames/drawing/src/runtime/guards/index.ts:53, packages/minigames/drawing/src/runtime/guards/index.ts:67, packages/minigames/drawing/src/runtime/guards/index.ts:87, packages/minigames/drawing/src/runtime/guards/index.ts:112, packages/minigames/drawing/src/runtime/guards/index.ts:122, packages/minigames/drawing/src/runtime/guards/index.ts:146, packages/minigames/drawing/src/runtime/guards/index.ts:163, … 7 more (run `work grep`)
- `isPlayersContentFile` → apps/server/src/contentLoader/loadPlayers/index.ts:2, apps/server/src/contentLoader/loadPlayers/index.ts:24
- `isTeamsContentFile` → apps/server/src/contentLoader/loadTeams/index.ts:2, apps/server/src/contentLoader/loadTeams/index.ts:25
- `isTriviaPrompt` → packages/minigames/trivia/src/runtime/content/index.ts:3, packages/minigames/trivia/src/runtime/content/index.ts:13
- `minigameType` → apps/client/src/App.tsx:39, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.test.tsx:12, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.test.tsx:30, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:16, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:19, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:42, … 113 more (run `work grep`)
- `prompt` → apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:142, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:156, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:186, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:217, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:347, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:348, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:352, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:362, … 153 more (run `work grep`)
- `promptId` → apps/client/src/components/HostControlPanel/MinigameSurface/index.test.tsx:128, packages/minigames/drawing/src/runtime/guards/index.ts:87, packages/minigames/drawing/src/runtime/guards/index.ts:122, packages/minigames/drawing/src/runtime/index.test.ts:304, packages/minigames/drawing/src/runtime/index.ts:257, packages/minigames/drawing/src/runtime/index.ts:308, packages/minigames/drawing/src/runtime/index.ts:309, packages/minigames/drawing/src/runtime/index.ts:313, … 18 more (run `work grep`)
- `rejected` → packages/minigames/drawing/src/client/HostDrawingSurface/DrawingCanvas/index.tsx:21, packages/minigames/drawing/src/client/HostDrawingSurface/DrawingCanvas/index.tsx:189, packages/minigames/drawing/src/runtime/index.test.ts:170, packages/shared/src/roomState/index.test-d.ts:232
- `resolveMinigameDefinition` → apps/server/src/roomState/selectors/index.ts:3, apps/server/src/roomState/selectors/index.ts:50, apps/server/src/roomState/selectors/index.ts:85
- `resolveMinigameTypeFromSlug` → apps/client/src/App.tsx:2, apps/client/src/App.tsx:24, apps/client/src/App.tsx:52, apps/client/src/minigames/registry/index.test.ts:6, apps/client/src/minigames/registry/index.test.ts:36, apps/client/src/minigames/registry/index.test.ts:40
- `rounds` → apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/copy.ts:21, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:68, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/index.tsx:95, apps/client/src/components/DisplayBoard/StageSurface/SetupStageBody/styles.ts:19, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:61, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:105, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:163, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:175, … 61 more (run `work grep`)
- `rulesKey` → apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:21, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:154, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/useConfigDraft.ts:158, apps/server/src/roomState/selectors/index.ts:52, apps/server/src/roomState/selectors/index.ts:56
- `runtimePlugin` → apps/client/src/components/MinigameDevSandbox/index.tsx:26, apps/client/src/components/MinigameDevSandbox/index.tsx:29, apps/client/src/components/MinigameDevSandbox/index.tsx:44, apps/client/src/components/MinigameDevSandbox/index.tsx:48, apps/client/src/components/MinigameDevSandbox/index.tsx:52, apps/client/src/components/MinigameDevSandbox/index.tsx:55, apps/client/src/components/MinigameDevSandbox/index.tsx:75, apps/client/src/components/MinigameDevSandbox/index.tsx:90, … 28 more (run `work grep`)
- `timers` → apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:254, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:255, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:278, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:279, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:302, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:303, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:210, apps/client/src/components/HostControlPanel/ConfigSetupPrototype/VariantA.tsx:35, … 21 more (run `work grep`)

**QA findings (advisory):** 3 finding(s) carried from the passing verdict:
- **minor** — packages/shared/src/content/promptPack/index.ts and packages/shared/src/content/validationIssue/index.ts are new modules with real branching logic (findDuplicateIdIssues, readPromptId, prefixIssuePaths' root-vs-nested branch) but have no colocated index.test.ts, violating testing.md's 'Colocate tests with the code under test: index.test.ts next to index.ts.' Coverage exists only indirectly via the trivia/drawing/players/teams consumer tests (verified: e.g. the trivia 'reports the later occurrence when two prompts share an id' test would fail without findDuplicateIdIssues), so there is no correctness gap, only a placement gap.
    evidence: packages/shared/src/content/promptPack/index.ts; packages/shared/src/content/validationIssue/index.ts (no sibling *.test.ts in either directory)
- **minor** — The one-line isObjectLike predicate (`typeof value === "object" && value !== null`) is copy-pasted verbatim across six new/changed files (validateGameConfigFile, players, teams, trivia, drawing, promptPack) instead of being centralized, even though this diff already created content/validationIssue/index.ts as a shared home for exactly this kind of reused, single-responsibility predicate. Per code-design.md: 'No copy-paste logic' and 'Extract a utility when the logic is independently testable, single-responsibility, OR reused.'
    evidence: e.g. packages/shared/src/content/gameConfig/validateGameConfigFile/index.ts:81-83, packages/shared/src/content/players/index.ts, packages/shared/src/content/teams/index.ts, packages/shared/src/content/trivia/index.ts, packages/shared/src/content/drawing/index.ts, packages/shared/src/content/promptPack/index.ts — identical `isObjectLike` definition in each
- **info** — packages/shared/src/content/gameConfig/validateGameConfigFile/index.ts is 278 lines, well past code-design.md's ~150-line 'look for a split' heuristic, and contains several independently-testable sub-validators (validateRound, validateScoring, validateTimers, validateMinigameRules) that are only exercised indirectly through the top-level validateGameConfigFile tests. Defensible as one coherent schema module (the heuristic is explicitly a prompt to look, not a hard cap), so flagged as info rather than minor.
    evidence: packages/shared/src/content/gameConfig/validateGameConfigFile/index.ts (278 lines)

_Captured 2026-08-07T05:08:03.548Z._
<!-- captured-evidence:end -->

## Links
Spawned from the 2026-08-05 config-wizard spike + prototype (Variant C picked). Downstream: WN-10 (server uses validator before writes), WN-11 (wizard renders issues inline).
