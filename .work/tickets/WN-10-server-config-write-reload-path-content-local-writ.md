---
# ─── Required ───────────────────────────────────────────────────────────────
id: WN-10
title: "Server config write + reload path (content/local writes, callable reload, config:* socket events)"
status: in-review
kind: feature
priority: medium
created: 2026-08-05

# ─── Optional (delete a line to take its default) ───────────────────────────
deps: [WN-9]                 # list<id>; DAG edges; must all be `done` before the SELECTOR picks this; default []
blocked_by: []           # list<string>; external/manual waits (free text); non-empty => selector skips; default []
# model: sonnet          # opus | sonnet | haiku; unset => global default-by-kind policy (SCHEMA §5)
# thinking: medium       # low | medium | high; unset => policy
# trust: checkpointed    # checkpointed | heads-down; default checkpointed
# needs_prototype: false # true => prototype must complete before in-progress; default false
# landing: preview-pr    # preview-pr | direct-main | feature-flag; unset => manifest default (SCHEMA §7)
# worktree:              # set by work-on on claim (collision guard); default null
# parallel_safe:         # RESERVED for F-8 (post-MVP) — do not set
worktree: "/Users/bradleyexton/Projects/wing-night-WN-10"
---

## Goal
Give the server a config write + reload path: atomic writes of edited content files into content/local/, loadContent extracted into a callable reload-and-re-seed operation, and authorized config:* socket events (read / save / apply) with an explicit SETUP-only apply lock.

## Acceptance Criteria
- [ ] A server `contentWriter` module writes a content file to `content/local/` (creating dirs as needed) atomically — write temp file, rename — refusing any payload whose shared validator (WN-9) returns issues: invalid content never lands on disk. It takes an injectable `contentRootDir` (the loaders already do), so tests never write into the real gitignored `content/local/`.
- [ ] **The gameConfig write path supplies the plugin-rules seam.** WN-9 landed `validateGameConfigFile` with rules validation as an *injected* option — called bare, `minigameRules` are not checked at all. `isRulesValidForKey` is currently a private `const` in `loadGameConfig/index.ts:48`. Lift it into its own server module exported for both callers, and have `contentWriter` pass it exactly as `loadGameConfig/index.ts:68` does. Without this, a config whose rules a plugin rejects passes the writer, lands in `content/local/`, and fatals every subsequent boot — with the wizard's own read path unusable to undo it. Test: a plugin-rejected `minigameRules` payload is refused and no file is written.
- [ ] The one-shot boot logic in `apps/server/src/index.ts` is extracted into a callable `reloadContentIntoRoomState()` operation (loadContent → setRoomStatePlayers/Teams/GameConfig/MinigameContent) used by both boot and apply; a reload failure surfaces through the existing fatalError path rather than crashing.
- [ ] New authorized socket events following the existing defineAuthorizedEvent + HostSecretPayload pattern: `config:read` returns the current merged content (gameConfig, players, teams, trivia + drawing packs, geo count) as loaded from disk; `config:save` validates + writes files (allowed in any phase); `config:apply` saves then reloads-and-re-seeds, hard-rejecting with a typed `CONFIG_LOCKED` error payload when `phase !== SETUP` (user-confirmed: save-while-locked allowed, apply rejected; Reset Game is the escape hatch).
- [ ] **`config:apply` success actually broadcasts — the naive wiring is a silent no-op.** `socketServer/index.ts:79` returns early on `if (!mutationResult.didMutate)`, and `didMutate` is raised only by `reportRoomStateMutation()`, which only `defineRoomMutation` calls. The four re-seed setters in `roomState/baseMutations/index.ts` (`setRoomStatePlayers/Teams/GameConfig/MinigameContent`) are plain functions that never raise it, so host and display would NOT see the new config. **Do not fix this by wrapping the re-seed in `defineRoomMutation`** — it early-returns on `isRoomInFatalState` (`defineRoomMutation/index.ts:30-32`), which is precisely the broken-content case this feature exists to repair. Instead `reloadContentIntoRoomState()` calls the plain setters and then calls `reportRoomStateMutation()` itself, outside the fatal gate. Test asserts an actual broadcast reaches a host and a display socket after apply, not merely that room state changed.
- [ ] **Apply from a fatal state clears it, without wiping live rosters.** A successful reload must clear `roomState.fatalError` — otherwise the server that booted fatal on bad content stays fatal forever and the wizard cannot repair anything, which is its whole purpose. Do NOT clear it via `overwriteRoomState(createInitialRoomState())`: that discards live SETUP rosters. Clear the field as part of the re-seed and state in the ticket which fields survive.
- [ ] A validation-rejected save/apply returns the `ValidationIssue[]` to the requesting socket.
- [ ] **No `config:*` handler can throw out of its socket.io listener.** The loaders throw by design (that is why boot wraps them), socket.io v4 dispatches listeners inside `process.nextTick` with no surrounding catch, and `apps/server` has no `process.on("uncaughtException")` anywhere — verified. So a `config:read` against a broken `content/local/` file takes the whole server down, in exactly the scenario the wizard is meant to fix. Every handler wraps its body and returns a typed error payload to the requesting socket instead. Test: with an unparseable local content file on disk, `config:read` returns an error response **and the process survives**.
- [ ] The response shapes are named explicitly. `defineAuthorizedEvent` cannot express these events as-is (no ack arity; `runMutation` must return `RoomState`), so state which mechanism carries the reply — ack callback or a `config:result`-style emit — since WN-11 builds its wizard against whatever this ticket picks.
- [ ] Server unit tests (contentLoader testHarness pattern) cover: atomic write + local-overrides-sample readback; apply rejected when locked; apply re-seeds room state (totalRounds recomputed); invalid payload rejected with issues and no file written.
- [ ] `pnpm typecheck` and `pnpm test` pass.

## Plan
Re-planned 2026-08-07 after the gate1 park (WN-10.gate1.json). All three majors were re-verified
against the landed code before answering — every line reference below was read, not inferred:

- **Broadcast no-op** — confirmed. `socketServer/index.ts:79` is the `didMutate` early return;
  `reportRoomStateMutation` lives in `roomState/mutationResult/index.ts:13` and is called only from
  `defineRoomMutation/index.ts:39`; the four setters in `baseMutations/index.ts:134-170` are plain
  functions. The trap is real too: `defineRoomMutation/index.ts:30-32` early-returns on
  `isRoomInFatalState`, so the obvious fix is unavailable. Hence the explicit
  "call `reportRoomStateMutation()` from the reload, outside the fatal gate" instruction.
- **Rules-validation hole** — confirmed. `isRulesValidForKey` is a private `const` at
  `loadGameConfig/index.ts:48`, passed as `validateRules` at line 68. This is a direct consequence of
  the injected seam WN-9 shipped: correct design, but the implication for the write path was never
  carried into this ticket. Lifting it to a shared module is the fix.
- **Listener crash** — confirmed. `grep uncaughtException apps/server/src` returns nothing.

Root-cause note: all three are the same failure — ACs written against how the server was *imagined*
rather than how it is *built*. The standing fix is to verify each AC against current code at
planning time, not to discover it at gate1 one ticket per iteration.

Grilled 2026-08-05 (plan-work session; user at the table).

- Transport DECIDED: socket events, not HTTP — reuses HostSecretPayload + defineAuthorizedEvent + broadcast; no express.json/HTTP-auth surface. v1 rides the existing hostSecret (WN-12 later swaps the gate to adminSecret).
- Apply semantics DECIDED: hard reject `CONFIG_LOCKED` when past SETUP; file saves permitted while locked (prep next week's config mid-night). Documented rule: Apply re-seeds room state and overwrites live setup:* edits (players/teams) — pre-flight wins pre-night, deck wins in-room after.
- `config:read` must read the merged current files (content/local/ wins), NOT just echo room state — `import:geo` may have written local files the room never saw; prompt packs aren't in room state at all.
- Watch the host-secret invalidation edge: reload does not touch hostAuth; the /admin socket and /host socket both claim HOST (last-claim-wins) — same-device usage is fine, but note the claim-war behavior in Progress if it bites during testing.
- Roster DECIDED in scope: wizard writes players.json/teams.json via the same save path.

## Progress
<the executing agent appends here — the restart-safe log>
- 2026-08-07T05:18:42.559Z gate1 (product-owner critic): needs-changes — recorded at .work/verdicts/WN-10.gate1.json. Summary: "Well-formed, correctly scoped, machine-checkable slice, but three ACs collide with landed code in ways that would land silently: the apply broadcast is a no-op through the existing pipeline, gameConfig saves skip the plugin rules seam, and config:* handlers can throw a socket.io listener exception that kills the process." MAJOR 1 (AC4 broadcast is a silent no-op): the broadcast pipeline gates on `didMutate`, a module-scoped flag raised ONLY by defineRoomMutation (roomState/mutationResult/index.ts:11-27; socketServer/index.ts:76-84). The four re-seed setters AC2 names (setRoomStatePlayers/Teams/GameConfig/MinigameContent, baseMutations/index.ts:134-184) are plain functions that never call reportRoomStateMutation(), so applyRoomStateMutation returns didMutate:false and broadcastAfter returns early — host + display would NOT see the new config without a refresh, which is exactly what AC4 promises. The obvious fix is also trapped: defineRoomMutation early-returns when isRoomInFatalState (defineRoomMutation/index.ts:30-40), i.e. it refuses to run in the broken-content state the wizard exists to repair. AC4 must name how apply reports its mutation, and AC5 must add a case asserting a snapshot is actually broadcast after a successful apply. MAJOR 2 (AC1 validation gate has a hole for gameConfig): WN-9 landed validateGameConfigFile with the per-plugin rules check as an INJECTED seam — called bare, minigameRules are not validated at all (validateGameConfigFile/index.ts:55-60 "Omitted => no rules issues"). The loader supplies it via isRulesValidForKey, which is PRIVATE to loadGameConfig (loadGameConfig/index.ts:44-69). So a gameConfig.json with plugin-rejected rules passes the writer, lands in content/local/, and fatals the very next load — and because the file persists, EVERY subsequent boot fatals, with the wizard read path unusable to undo it. AC1 must require the writer to validate gameConfig through the same plugin-backed rules seam (extract isRulesValidForKey so there is one implementation), and AC5 must add a bad-minigameRules case. MAJOR 3 (no AC covers throwing loaders inside socket.io listeners): loadContent and every loader under it throw (loadContentFileWithFallback/index.ts:27-29; contentLoaderUtils/index.ts:14-20) — which is why boot wraps them in try/catch (index.ts:22-41). socket.io v4 dispatches listeners inside process.nextTick with no surrounding catch, and there is no process.on("uncaughtException") anywhere in apps/server, so a throw out of config:read/save/apply takes the server DOWN. The acute case is the one the feature exists for: with a broken content/local file the server boots into fatalError (alive, by design), then the wizard config:read re-reads that same broken file and kills the process. Needs an AC that config:read/save/apply never throw out of the handler — a load/parse failure returns a typed error payload distinct from CONFIG_LOCKED — plus a matching AC5 case. MINOR (AC3 pattern cannot express these events): defineAuthorizedEvent requires runMutation to return RoomState and the dispatch discards the result (registerRoomStateHandlers/index.ts:80-117), no ack/callback arity exists anywhere in the contract, and the only per-socket server->client precedent is the payload-less host:secretInvalid — so new SERVER_TO_CLIENT_EVENTS + a non-mutating registration path must be minted. Graded minor (it fails typecheck rather than landing silently) but the response event names/shapes are the contract WN-11 is written against, so the ticket should name them. MINOR (injectable content root): every loader takes options.contentRootDir and the contentLoader testHarness writes into mkdtempSync(os.tmpdir()); if contentWriter/reloadContentIntoRoomState hard-code the default root, AC5 tests write into the repo real content/local/ — gitignored, so it survives unnoticed and shadows content/sample for the dev server. AC1/AC2 should state both new modules accept the contentRootDir override. MINOR (AC2 fatalError path is destructive): setRoomStateFatalError first does overwriteRoomState(createInitialRoomState()) (baseMutations/index.ts:112-132), so a failed apply-time reload WIPES players/teams entered live in SETUP. Bounded (apply is SETUP-only, writes pre-validated, setup baseline survives), but the ticket should say the wipe is intended or scope apply-time failure to a non-destructive error return. Checks: well-formed ok, machine-checkable-finish ok, scoped ok, blast-radius ok, hidden-constraints FAILED, worth-doing ok. Confidence high. Routing per ship-next gate1: explicit rejection => demote ready -> needs-planning and re-plan via plan-work. Pipeline halted before implement; no code was written.
- 2026-08-07T11:16:34.980Z gate1 round 2 (product-owner critic, re-judged after the f3d68c8 re-plan): PASS — recorded at .work/verdicts/WN-10.gate1.json (attempt 2, supersedes the 01:18 needs-changes). All three round-1 MAJORs verified fixed against landed code: (1) the AC5 reportRoomStateMutation escape genuinely raises didMutate — the flag is reset at the TOP of applyRoomStateMutation (mutationResult/index.ts:20) and read right after runMutation returns (line 25); (2) the isRulesValidForKey lift is feasible — it depends only on the shared MINIGAME_DEFINITIONS-derived key map and apps/server/src/minigames/registry, both liftable with no import cycle, and validateGameConfigFile takes a pre-narrowing unknown so the writer can call it exactly as loadGameConfig/index.ts:67-69 does; (3) loadContent and every loader under it are fully SYNCHRONOUS, so AC7's per-handler try/catch really does close the uncaughtException route. Checks: all six ok (hidden-constraints flipped from FAILED to ok). Confidence high. FIVE MINORS RIDE AS IMPLEMENTATION NOTES — read these before building: (a) AC5's escape only broadcasts if reloadContentIntoRoomState() runs INSIDE the dispatch thunk's dynamic extent; if config:apply is registered solely on AC8's new non-mutating reply path, applyRoomStateMutation is never called, the flag is raised with nobody reading it, and socketServer/index.ts:79 still never broadcasts — route the re-seed through context.dispatchAuthorizedMutation/broadcastAfter and emit the reply separately. (b) AC5's 'broadcast reaches a host and a display socket' test has NO harness: apps/server has no socket.io-client dep, no test boots a real Server, and the registerRoomStateHandlers testHarness spy never invokes the thunk. Asserting at the applyRoomStateMutation(runMutation).didMutate === true seam is a sufficient proof chain (socketServer/index.ts:79-83 is one line later) and WN-11's Playwright spec covers it end-to-end — take the seam assertion rather than growing scope with a port-binding integration test; do NOT weaken the assertion instead. (c) the injectable contentRootDir landed on AC1 only — thread it through reloadContentIntoRoomState (AC3) and the config:read path too, or AC7's unparseable-file test writes into the repo's real gitignored content/local/ and silently shadows content/sample for the dev server. (d) AC3 still routes apply-time reload failure through setRoomStateFatalError, whose first act is overwriteRoomState(createInitialRoomState()) — which wipes the very SETUP rosters AC6 protects; prefer a non-destructive typed error return alongside AC7's error payloads. (e) AC6's 'without wiping live rosters' is a constraint on the fatalError CLEARING mechanism, not on the re-seed — the Plan's decided semantics stand (apply overwrites live setup:* rosters, pre-flight wins pre-night); do not add roster-preservation logic, and add a roster assertion to AC9.
- 2026-08-07T11:16:37.804Z prototype: skipped (not in plan)
- 2026-08-07T11:17:19.536Z claimed → in-progress @ /Users/bradleyexton/Projects/wing-night-WN-10
- 2026-08-07T11:19:24.473Z Explored the implicated code; design decided before writing any of it. AC8 DECISION (the contract WN-11 builds against): replies ride a NEW server->client emit SERVER_TO_CLIENT_EVENTS.CONFIG_RESULT = 'config:result', NOT an ack callback — every ClientToServerEvents entry is single-arg and index.test-d.ts pins those Parameters tuples, so an ack arity would be the first multi-arg entry in the contract and a wider change than this ticket needs. Shape: a discriminated union on ok — { action, ok: true, content } | { action, ok: false, code, message, issues }. Module plan: (1) lift isRulesValidForKey + MINIGAME_TYPE_BY_RULES_KEY out of loadGameConfig into apps/server/src/minigames/rulesValidation/ (colocated with the registry it resolves plugins through), imported by BOTH loadGameConfig and contentWriter — one implementation of the rule, per AC2; (2) apps/server/src/contentWriter/ — key-to-{fileName,validate} table over the five editable files, validates then writes temp+rename, takes contentRootDir; (3) apps/server/src/reloadContentIntoRoomState/ — loadContent -> the four setters -> clearRoomStateFatalError -> reportRoomStateMutation(), RETURNING a typed result instead of throwing. That return-not-throw shape is what resolves minor (d) without breaking AC3: the reload module owns the reload, and each CALLER picks its own failure policy — boot still routes a failure to setRoomStateFatalError (AC3 satisfied, fail-fast preserved), while config:apply returns a typed error payload and leaves live SETUP rosters intact (minor (d) satisfied). Handling minor (a) explicitly: config:apply routes its re-seed through context.dispatchAuthorizedMutation's thunk so reportRoomStateMutation() fires inside applyRoomStateMutation's reset/read window (mutationResult/index.ts:20-25) and socketServer/index.ts:79 actually broadcasts; config:read and config:save mutate nothing and emit their reply directly without touching the dispatch.
- 2026-08-07T11:31:25.951Z Implemented and verified. Modules: minigames/rulesValidation (lifted isRulesValidForKey + MINIGAME_TYPE_BY_RULES_KEY, now imported by loadGameConfig AND contentWriter), contentWriter (validate-all-then-write-any, temp+rename, injectable contentRootDir), reloadContentIntoRoomState (returns a typed result; boot keeps the destructive fatalError policy, apply gets a non-destructive typed error), readConfigContent (merged on-disk read, non-throwing), configService (read/save/reload, all contentRootDir-injectable), clearRoomStateFatalError (clears the field ONLY — no overwriteRoomState, so live SETUP rosters survive the repair), and the config:* handlers. AC8 shipped as config:result emits. MUTATION-TESTED the two assertions that matter rather than trusting them: (1) deleting reportRoomStateMutation() from the reload turns BOTH broadcast tests red ('reports its mutation so the broadcast gate opens after a re-seed' + 'config:apply reports a mutation so host and display are broadcast to'), so the AC5 no-op cannot silently return; (2) dropping validateRules:isRulesValidForKey from contentWriter turns 'refuses a gameConfig whose minigameRules the owning plugin rejects' red, so the AC2 seam cannot silently detach. Both restored and green after. On minor (b): took the didMutate seam assertion the gate1 critic blessed rather than adding a socket.io-client dependency — socketServer/index.ts:79-83 broadcasts to the host and display rooms one line after reading that flag, and I added createRealMutationDispatch to the harness so the test runs the REAL applyRoomStateMutation instead of the old spy that never invoked the thunk. Also added a negative test pinning didMutate false without a report, so the positive test cannot pass vacuously. Verify green: lint + typecheck + test, 183 server tests (26 new), 0 failures.
- 2026-08-07T11:34:21.135Z Anti-blind-spot sweep done (work grep --since main). Most flagged symbols were generic locals (roomState, snapshot, issues, minigameType) — grep noise, not call-sites of anything I changed. The real blast radius is three things, each opened and checked: (1) CLIENT_TO_SERVER_EVENTS / SERVER_TO_CLIENT_EVENTS gained members — verified NOTHING iterates either map exhaustively (no Object.keys/values over them anywhere outside node_modules), and the client's hostRequestTable is keyed by the hand-maintained HostRequestArgs rather than by the event union, so config:* widens PayloadEventName without demanding new table entries; the three client consumers (hostRequests, wireHostControlClaim, wireRoomStateRehydration) all reference specific named events. apps/client typecheck green is the machine proof. (2) registerRoomStateHandlers gained an OPTIONAL 6th param defaulting to the production service, so its one production call site (socketServer/index.ts) and all five test files via setupHandlers are unaffected. (3) apps/server/src/index.ts — the boot path I rewrote — has NO unit coverage, so I booted the real server twice rather than assert it works. Happy path: boots clean on :3199, health 200, no fatal (content loaded through the new reloadContentIntoRoomState). Broken-content path: with an unparseable content/local/gameConfig.json it logs 'server:contentLoadFailed Failed to parse game config content ...' and STILL starts, health 200 — i.e. the boot failure policy routes to fatalError and stays alive, which is the precondition the wizard repairs from. NOTE ON A FALSE START, recorded because the first result was misleading: my initial broken-content run returned 200 from the PREVIOUS server still holding the port while the new process died on EADDRINUSE — that run proved nothing. Re-ran on a clean port after a hard kill; the result above is the clean one. Temp content/local/ removed; worktree clean.
- 2026-08-07T11:45:08.124Z qa-reviewer round 1: needs-changes with a BLOCKER — and it was a real one I missed, not a false positive. contentWriter's default content root resolved ONE DIRECTORY TOO HIGH. resolveDefaultContentRootDir hardcoded a five-levels-up walk calibrated for the loaders, which sit two dirs below src (src/contentLoader/<loader>/); contentWriter sits one (src/contentWriter/). Measured it rather than reasoning about it: loader default root = <repo>/content, writer default root = /Users/bradleyexton/Projects/content — the repo's PARENT. Production wires no override (socketServer/index.ts passes 5 args, so createConfigService() runs with {}), and writeFileAtomically mkdirSyncs recursively, so config:save would have created a content/local/ OUTSIDE the repo while the read half — which delegates to loadContent's correctly-calibrated per-loader defaults — kept reading the untouched real files. Net effect: apply writes nowhere useful, re-seeds stale content, emits ok:true, and broadcasts a no-change snapshot. Invisible to my whole suite because every test injects contentRootDir and never exercises the default. FIX went at the class of bug, not the instance: resolveDefaultContentRootDir is DELETED and replaced by a single DEFAULT_CONTENT_ROOT_DIR constant computed once in contentLoaderUtils and imported by all five consumers (four loaders + the writer), so no caller re-derives a root from its own depth and the divergence is now unrepresentable. Added the guard qa said was missing (contentLoaderUtils/index.test.ts) and mutation-tested it: widening the walk by one level turns BOTH new tests red; restored, green. Also fixed the three minors and one info: (a) CONFIG_ERROR_CODES.WRITE_FAILED was declared but never emitted while fs errors were mislabeled LOAD_FAILED — writeContentFiles now returns a discriminated {reason:'invalid'|'writeFailed'} and configService maps them to distinct codes, with a test forcing ENOTDIR; (b) the config:read guard claimed 'payload is ConfigSavePayload' while only checking the host-secret envelope — defineConfigEvent is now generic over the payload and read is typed ConfigReadPayload; (c) the handler-level try/catch had no isolated coverage (deleting it left the suite green because the inner layers each catch) — added a test with a configService stub whose read() throws, which fails if that outer guard is removed; (d) restored the boot catch scope, which I had narrowed to loadContent only — it now spans the setters too, matching the sequence it replaced. Verify green: lint + typecheck + test, 187 server tests (was 183), 0 failures. Shared root re-probed: <repo>/content for loaders AND writer.
- 2026-08-07T11:59:06.486Z browser-verify: skipped (non-UI)
- 2026-08-07T11:59:06.595Z qa-reviewer round 3 (re-grade of the minor-fix delta at 20f2b8d): PASS — recorded at .work/verdicts/WN-10.qa.json. Confirmed the reloadContentIntoRoomState change is mechanically comment-only, re-ran both mutation checks on the changed tests (swapping WRITE_FAILED->LOAD_FAILED turns the new configService mapping test red; shifting the content-root walk in EITHER direction turns the rewritten guard red, so the determinism rewrite did not trade away the blocker's coverage), and confirmed the determinism fix by running the suite green with a broken content/local present where the previous version went red. Full gate re-run by the reviewer: 517 tests pass / 0 fail across all packages, typecheck and lint exit 0. TWO ADVISORY MINORS RIDE INTO EVIDENCE (not fixed, deliberately — they are non-blocking and further iteration here would be self-play): (1) no unit test now loads the repo's REAL default content root through the loaders — the rewrite asserts the six sample files exist rather than that they parse, so a corrupted content/sample/*.json would stay green in unit tests and surface only at boot or e2e; (2) that test hardcodes the six fallback filenames, duplicating a set the loaders own, so a seventh content file would not be noticed. Both are worth a follow-up ticket if the sample content ever becomes something people hand-edit.
- 2026-08-07T11:59:23.909Z handed off → in-review (verify green); awaiting land

## Evidence
<test output + screenshot / preview URL, recorded before `done`>

<!-- captured-evidence:start -->
**Verify gate:** ✓ PASS (3 step(s))

```
✓ lint: pnpm lint
✓ typecheck: pnpm typecheck
✓ test: pnpm test
```

**Anti-blind-spot grep:** 19 symbol(s) with external call-sites reviewed (1 low-signal name(s) skipped: result):

- `action` → apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.test.tsx:27, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.test.tsx:46, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.tsx:10, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.tsx:29, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.tsx:133, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.tsx:145, apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.tsx:157, apps/client/src/components/HostControlPanel/index.test.tsx:107, … 18 more (run `work grep`)
- `contentRoot` → apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:8, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:11, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:12, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:15, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:29, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:32, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:35, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:49, … 65 more (run `work grep`)
- `contentRootDir` → apps/server/src/contentLoader/index.ts:10, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:15, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:35, apps/server/src/contentLoader/loadContentFileWithFallback/index.test.ts:54, apps/server/src/contentLoader/loadContentFileWithFallback/index.ts:5, apps/server/src/contentLoader/loadContentFileWithFallback/index.ts:12, apps/server/src/contentLoader/loadContentFileWithFallback/index.ts:16, apps/server/src/contentLoader/loadContentFileWithFallback/index.ts:17, … 23 more (run `work grep`)
- `drawingContent` → packages/minigames/drawing/src/runtime/index.ts:65, packages/minigames/drawing/src/runtime/index.ts:71, packages/minigames/drawing/src/runtime/index.ts:228, packages/minigames/drawing/src/runtime/index.ts:229, packages/minigames/drawing/src/runtime/index.ts:269, packages/minigames/drawing/src/runtime/index.ts:270, packages/minigames/drawing/src/runtime/index.ts:305, packages/minigames/drawing/src/runtime/index.ts:306
- `exists` → apps/client/src/components/HostControlPanel/OverrideActionsSurface/index.test.tsx:27, apps/client/src/components/HostControlPanel/index.tsx:31, apps/client/src/utils/hostRequests/index.test.ts:185, apps/server/src/contentLoader/loadGameConfig/index.test.ts:40, apps/server/src/contentLoader/loadPlayers/index.test.ts:52, apps/server/src/contentLoader/loadTeams/index.test.ts:43, tools/playwright-ports/index.mjs:9
- `geoContent` → packages/minigames/geo/src/runtime/index.ts:38, packages/minigames/geo/src/runtime/index.ts:50, packages/minigames/geo/src/runtime/index.ts:70, packages/minigames/geo/src/runtime/index.ts:71, packages/minigames/geo/src/runtime/index.ts:153, packages/minigames/geo/src/runtime/index.ts:155, packages/minigames/geo/src/runtime/index.ts:185, packages/minigames/geo/src/runtime/index.ts:187, … 1 more (run `work grep`)
- `harness` → apps/client/src/utils/hostRequests/index.test.ts:45, apps/client/src/utils/hostRequests/index.test.ts:48, apps/client/src/utils/hostRequests/index.test.ts:49
- `issues` → packages/shared/src/content/drawing/index.test.ts:11, packages/shared/src/content/drawing/index.test.ts:12, packages/shared/src/content/drawing/index.test.ts:15, packages/shared/src/content/drawing/index.test.ts:43, packages/shared/src/content/drawing/index.test.ts:45, packages/shared/src/content/drawing/index.test.ts:46, packages/shared/src/content/drawing/index.test.ts:55, packages/shared/src/content/gameConfig/validateGameConfigFile/index.test.ts:36, … 76 more (run `work grep`)
- `lastResult` → apps/client/src/components/HostControlPanel/MinigameSurface/index.test.tsx:88, apps/client/src/components/HostControlPanel/MinigameSurface/index.test.tsx:127, apps/server/src/roomState/minigamePlayMutations.test.ts:122, packages/minigames/geo/src/client/HostGeoSurface/index.tsx:161, packages/minigames/geo/src/client/HostGeoSurface/index.tsx:164, packages/minigames/geo/src/client/HostGeoSurface/index.tsx:169, packages/minigames/geo/src/runtime/guards/index.ts:101, packages/minigames/geo/src/runtime/index.test.ts:104, … 15 more (run `work grep`)
- `minigameType` → apps/client/src/App.tsx:39, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.test.tsx:12, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.test.tsx:30, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:16, apps/client/src/components/DisplayBoard/StageSurface/MinigameIntroStageBody/index.tsx:19, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:10, apps/client/src/components/DisplayBoard/StageSurface/MinigameStageBody/index.tsx:42, … 115 more (run `work grep`)
- `mutationResult` → apps/server/src/roomState/defineRoomMutation/index.ts:4, apps/server/src/roomState/mutationResult/index.test.ts:15, apps/server/src/roomState/mutationResult/index.test.ts:17, apps/server/src/roomState/mutationResult/index.test.ts:18, apps/server/src/roomState/mutationResult/index.test.ts:24, apps/server/src/roomState/mutationResult/index.test.ts:26, apps/server/src/roomState/mutationResult/index.test.ts:27, apps/server/src/socketServer/index.ts:77, … 2 more (run `work grep`)
- `of` → apps/client/src/components/DisplayBoard/StageSurface/EatingStageBody/styles.test.ts:12, apps/client/src/components/DisplayBoard/StageSurface/EatingStageBody/styles.ts:38, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:57, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:148, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:189, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:196, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:244, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:248, … 125 more (run `work grep`)
- `reloaded` → tests/e2e/refresh-rehydrate.spec.ts:60, tests/e2e/refresh-rehydrate.spec.ts:75
- `roomState` → apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:39, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:43, apps/client/src/components/DisplayBoard/StageSurface/index.tsx:158, apps/client/src/components/DisplayBoard/StageSurface/index.tsx:159, apps/client/src/components/DisplayBoard/StageSurface/index.tsx:164, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:80, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:82, apps/client/src/components/DisplayBoard/StageSurface/resolveStageViewModel/index.ts:84, … 338 more (run `work grep`)
- `runtimePlugin` → apps/client/src/components/MinigameDevSandbox/index.tsx:26, apps/client/src/components/MinigameDevSandbox/index.tsx:29, apps/client/src/components/MinigameDevSandbox/index.tsx:44, apps/client/src/components/MinigameDevSandbox/index.tsx:48, apps/client/src/components/MinigameDevSandbox/index.tsx:52, apps/client/src/components/MinigameDevSandbox/index.tsx:55, apps/client/src/components/MinigameDevSandbox/index.tsx:75, apps/client/src/components/MinigameDevSandbox/index.tsx:90, … 24 more (run `work grep`)
- `snapshot` → apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:248, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:273, apps/client/src/components/DisplayBoard/StageSurface/index.test.tsx:297, apps/client/src/components/DisplayBoard/index.test.tsx:35, apps/client/src/components/DisplayBoard/index.test.tsx:51, apps/client/src/components/HostControlPanel/CompactSummarySurface/index.test.tsx:30, apps/client/src/components/HostControlPanel/MinigameSurface/index.test.tsx:62, apps/client/src/components/HostControlPanel/MinigameSurface/index.tsx:60, … 296 more (run `work grep`)
- `that` → apps/server/src/contentLoader/loadGameConfig/index.test.ts:171, apps/server/src/roomState/mutationResult/index.ts:10, apps/server/src/roomState/scoringMutations/index.ts:197, packages/shared/src/content/gameConfig/validateGameConfigFile/index.ts:56, packages/shared/src/content/gameConfig/validateGameConfigFile/index.ts:80, packages/shared/src/content/promptPack/index.ts:8, packages/shared/src/content/validationIssue/index.ts:3, tests/e2e/refresh-rehydrate.spec.ts:22, … 5 more (run `work grep`)
- `triviaContent` → packages/minigames/trivia/src/runtime/index.ts:64, packages/minigames/trivia/src/runtime/index.ts:74, packages/minigames/trivia/src/runtime/index.ts:160, packages/minigames/trivia/src/runtime/index.ts:162, packages/minigames/trivia/src/runtime/index.ts:164, packages/minigames/trivia/src/runtime/index.ts:179, packages/minigames/trivia/src/runtime/index.ts:180, packages/minigames/trivia/src/runtime/index.ts:187, … 1 more (run `work grep`)
- `validGameConfig` → packages/shared/src/content/gameConfig/validateGameConfigFile/index.test.ts:19, packages/shared/src/content/gameConfig/validateGameConfigFile/index.test.ts:32, packages/shared/src/content/gameConfig/validateGameConfigFile/index.test.ts:41, packages/shared/src/content/gameConfig/validateGameConfigFile/index.test.ts:190

**QA findings (advisory):** 2 finding(s) carried from the passing verdict:
- **minor** — Coverage narrowed slightly, in a direction unrelated to the ticket's blocker: the old test proved the repo's real sample content is actually LOADABLE (loadContent() parses + validates it); the rewrite only proves the six files EXIST. Nothing in the default `pnpm test` gate now loads the repo's real default content root, so a corrupted content/sample/*.json would leave unit tests green and only surface at server boot / e2e. This does not weaken the original blocker's guard (see checks) and the trade bought real determinism, so it is advisory, not blocking.
    evidence: Old: `const content = loadContent(); assert.ok(content.gameConfig.rounds.length > 0)`. New: `assert.equal(existsSync(resolve(DEFAULT_CONTENT_ROOT_DIR, 'sample', fileName)), true, ...)` at apps/server/src/contentLoader/contentLoaderUtils/index.test.ts:45. `grep -rln 'loadContent('` over apps/server/src now shows only tests that inject a temp root (contentLoader/index.test.ts, contentWriter/index.test.ts) — no unit test exercises the real DEFAULT_CONTENT_ROOT_DIR through the loaders. testing.md §Test quality ("Keep tests deterministic") justifies the rewrite; the residual gap is noted, not charged.
- **info** — The rewritten test hardcodes the six fallback filenames as a literal list, duplicating a set that the loaders own (loadPlayers/loadTeams/loadGameConfig contentFileName + the three minigame files). If a seventh content file is ever added, the test will not know. Verified today the list is exactly complete and correct.
    evidence: `find content/sample -type f` returns exactly the six files in sampleFileNames; loaders' names confirmed at apps/server/src/contentLoader/loadPlayers/index.ts:53, loadTeams/index.ts:45, loadGameConfig/index.ts:73. code-design §Utilities & extraction — incidental duplication, non-trivial only if the set grows.

_Captured 2026-08-07T11:59:23.909Z._
<!-- captured-evidence:end -->

## Links
Deps: WN-9 (validators). Downstream: WN-11 (wizard consumes config:* events). Related: geo import CLI writes content/local/minigames/geo.json (tools/import-geo-photos).
